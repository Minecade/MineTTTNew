package src.main.java.de.orion304.ttt.players;


public class RecentDamage {

	// private static ConcurrentHashMap<String, String> recentDamage = new
	// ConcurrentHashMap<>();
	//
	// public RecentDamage(Player receiver, Player dealer) {
	// recentDamage.put(receiver.getName(), dealer.getName());
	// }
	//
	// public static Player getDamager(Player target) {
	// String name = target.getName();
	// if (recentDamage.containsKey(name)) {
	// String culprit = recentDamage.get(name);
	// Player player = Bukkit.getServer().getPlayer(culprit);
	// return player;
	// }
	// return null;
	// }

}
